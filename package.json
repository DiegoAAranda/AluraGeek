{
  "name": "alurageek",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:json-server": "json-server --watch db.json --port 3001",
    "start:dev": "browser-sync start --server --file . --port 3000 --startPath index.html",
    "start": "npm-run-all --parallel start:json-server start:dev"
  },
  "keywords": [],
  "author": "Diego Aldair Aranda Zavaleta",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "npm-run-all": "^4.1.5"
  }
}
